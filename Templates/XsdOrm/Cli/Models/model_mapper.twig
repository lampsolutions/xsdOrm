
    /**
    * map {{attribute.attribute}}
    * @param $targetObject
    */
    public function map{{attribute.method|capitalize}}(&$targetObject){
        $targetObject->{{ attribute.setMapping}}($this->{{attribute.attribute}});
    }
